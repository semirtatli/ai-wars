import { SetupForm } from '@/components/setup/setup-form';

/**
 * Home page — Battle setup screen.
 * Server Component shell that renders the client-side SetupForm.
 */
export default function HomePage() {
  return <SetupForm />;
}

